{
  "experiment_name": "17_best_combined",
  "description": "Best practices combined - wider network, dropout, batch norm, LR scheduler",
  "hidden_layers": [256, 128],
  "batch_size": 128,
  "learning_rate": 0.01,
  "optimizer": "adam",
  "epochs": 15,
  "dropout": 0.2,
  "batch_norm": true,
  "weight_decay": 0.0001,
  "data_augmentation": true,
  "lr_scheduler": true,
  "scheduler_type": "cosine"
}
