{
  "experiment_name": "12_reg_l2",
  "description": "L2 regularization (weight_decay=0.001)",
  "hidden_layers": [128, 64],
  "batch_size": 128,
  "learning_rate": 0.001,
  "optimizer": "adam",
  "epochs": 10,
  "dropout": 0.0,
  "batch_norm": false,
  "weight_decay": 0.001,
  "data_augmentation": false,
  "lr_scheduler": false
}
