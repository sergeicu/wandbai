{
  "experiment_name": "00_baseline",
  "description": "Baseline MNIST classifier with standard settings",
  "hidden_layers": [128, 64],
  "batch_size": 128,
  "learning_rate": 0.001,
  "optimizer": "adam",
  "epochs": 10,
  "dropout": 0.0,
  "batch_norm": false,
  "weight_decay": 0.0,
  "data_augmentation": false,
  "lr_scheduler": false
}
