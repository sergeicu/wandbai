{
  "experiment_name": "14_scheduler_cosine",
  "description": "Cosine annealing scheduler - smooth LR decay",
  "hidden_layers": [128, 64],
  "batch_size": 128,
  "learning_rate": 0.01,
  "optimizer": "adam",
  "epochs": 15,
  "dropout": 0.0,
  "batch_norm": false,
  "weight_decay": 0.0,
  "data_augmentation": false,
  "lr_scheduler": true,
  "scheduler_type": "cosine"
}
